#!/usr/bin/env python
#-*- coding:utf-8 -*-

1、调用商城入口
    调用登录模块，3次锁定成功后进入商城
2、进入商城后，购买物品然后加入到购物撤
    用户输入退出指令，调用银行卡信息，然后判断剩余余额是否足够
        足够，减去消费金额，并把购物车写入至文件中
            判断文件是否存在存在追加，不存在重新创建，用username+月份创建文件
