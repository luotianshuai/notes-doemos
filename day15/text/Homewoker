homewoker:
	1、已经进入编辑模式
		全选：
			先找到所有的tr，然后循环每个tr
			在去tr中检查checkbox是否已经选中
				全选：
						原来td中仅仅是一个字符串，如果我把字符串变成一个input标签，然后value=td中的内容。他就进入编辑模式了
									这个操作就是：把td中的文本替换成<input value='当前tr中的文本内容'>
									文本替换：
											通过$(...).text()就可以获取到里面的内容
											var old = $(xxx).text();
											var tem="<input value='"+old+"'>"
											$(...).html(temp)


				取消：
						把<input value='当前td文本'> 这个标签替换成文本


	2、单选、反选  （可选作业）